import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericMachine } from './generic-machine.js';

// PLACEHOLDER: draw the real "Excavator" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `excavator.ts` and fix the import in `../category-icons.ts`.
export const excavator: CategoryIconGlyph = {
  key: 'excavator',
  label: 'Excavator',
  paths: genericMachine.paths,
};
