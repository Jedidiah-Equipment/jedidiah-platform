import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericImplement } from './generic-implement.js';

// PLACEHOLDER: draw the real "Plough" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `plough.ts` and fix the import in `../category-icons.ts`.
export const plough: CategoryIconGlyph = {
  key: 'plough',
  label: 'Plough',
  paths: genericImplement.paths,
};
