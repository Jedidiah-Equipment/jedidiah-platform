import { TruckIcon } from 'lucide-react';
import type React from 'react';

export function DispatchDepartmentIcon(props: React.ComponentProps<typeof TruckIcon>) {
  return <TruckIcon {...props} />;
}
