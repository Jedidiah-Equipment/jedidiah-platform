import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericImplement } from './generic-implement.js';

// PLACEHOLDER: draw the real "Planter" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `planter.ts` and fix the import in `../category-icons.ts`.
export const planter: CategoryIconGlyph = {
  key: 'planter',
  label: 'Planter',
  paths: genericImplement.paths,
};
