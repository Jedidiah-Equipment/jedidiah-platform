import { PackageCheckIcon } from 'lucide-react';
import type React from 'react';

export function SupplyDepartmentIcon(props: React.ComponentProps<typeof PackageCheckIcon>) {
  return <PackageCheckIcon {...props} />;
}
