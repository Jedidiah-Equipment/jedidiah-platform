# Phase 0 namespace moves (ADR 0016). Entries must be the squash commits that landed on main, never
# the pre-merge branch commits: `git blame --ignore-revs-file` only honours reachable history, and a
# test in pkg/api verifies every hash below is an ancestor of HEAD.

# refactor(namespaces): isolate equipment application (#1416)
5aaa8a921f73f87bbc8f0b2681a79de16243eae1
# refactor(namespaces): classify frontend equipment features (#1417)
0337707c
# refactor(db): move equipment tables into named schema (#1418)
08b59dba
