import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericImplement } from './generic-implement.js';

// PLACEHOLDER: draw the real "Slasher" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `slasher.ts` and fix the import in `../category-icons.ts`.
export const slasher: CategoryIconGlyph = {
  key: 'slasher',
  label: 'Slasher',
  paths: genericImplement.paths,
};
