{
  "name": "@pkg/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --port ${PORT:-5173}",
    "build": "vitepress build",
    "start": "vitepress preview --port ${PORT:-4173}",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "vitepress": "^1.6.4"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
