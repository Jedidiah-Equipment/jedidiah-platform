import { IconHelpCircle } from '@tabler/icons-react';
import type React from 'react';

/** The canonical circled-question-mark glyph for both navigation and contextual Help. */
export function HelpIcon(props: React.ComponentProps<typeof IconHelpCircle>) {
  return <IconHelpCircle {...props} />;
}
