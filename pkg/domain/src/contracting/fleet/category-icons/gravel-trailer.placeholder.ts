import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericImplement } from './generic-implement.js';

// PLACEHOLDER: draw the real "Gravel trailer" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `gravel-trailer.ts` and fix the import in `../category-icons.ts`.
export const gravelTrailer: CategoryIconGlyph = {
  key: 'gravel-trailer',
  label: 'Gravel trailer',
  paths: genericImplement.paths,
};
