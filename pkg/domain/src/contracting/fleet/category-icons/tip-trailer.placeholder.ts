import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericImplement } from './generic-implement.js';

// PLACEHOLDER: draw the real "Tip trailer" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `tip-trailer.ts` and fix the import in `../category-icons.ts`.
export const tipTrailer: CategoryIconGlyph = {
  key: 'tip-trailer',
  label: 'Tip trailer',
  paths: genericImplement.paths,
};
