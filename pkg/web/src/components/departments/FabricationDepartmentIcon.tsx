import { HammerIcon } from 'lucide-react';
import type React from 'react';

export function FabricationDepartmentIcon(props: React.ComponentProps<typeof HammerIcon>) {
  return <HammerIcon {...props} />;
}
