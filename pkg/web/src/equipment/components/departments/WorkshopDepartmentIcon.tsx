import { IconTools } from '@tabler/icons-react';
import type React from 'react';

export function WorkshopDepartmentIcon(props: React.ComponentProps<typeof IconTools>) {
  return <IconTools {...props} />;
}
