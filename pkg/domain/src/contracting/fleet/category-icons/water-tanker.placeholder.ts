import type { CategoryIconGlyph } from '../category-icon-glyph.js';
import { genericMachine } from './generic-machine.js';

// PLACEHOLDER: draw the real "Water tanker" glyph on the 24px / 2px-stroke grid, replace `paths`,
// then rename this file to `water-tanker.ts` and fix the import in `../category-icons.ts`.
export const waterTanker: CategoryIconGlyph = {
  key: 'water-tanker',
  label: 'Water tanker',
  paths: genericMachine.paths,
};
